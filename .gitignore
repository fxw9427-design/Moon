.DS_Store
Thumbs.db
*.swp
*.swo
*.bak
node_modules/
.vscode/
.idea/

# memories/ 不上传到 GitHub，保留在本地
memories/

# plaintext source — never commit, encrypted_data.js is the output
data.js

# toy control scripts — local only
toy_control.ps1
toy_bridge.mjs

# MCP config contains API keys — never commit
.mcp.json

# build scripts and secrets — local only, never commit
build.mjs
commit.mjs
lint_data.mjs
.secrets.json

# session drafts — local working memory, not for git
sessions/
